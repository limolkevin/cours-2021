{% extends 'base.html.twig' %}

{% block title %}Edit Myblog{% endblock %}

{% block body %}
    <h1 class="text-center">Edit Article</h1>
    <div class="container">
        <div class="row">
            <div class="col-4 mx-auto">
                
    {{ include('myblog/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('myblog_index') }}" class="btn btn-primary w-100">Retour</a>

    {{ include('myblog/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
